spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:bookdb-test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
    enabled: false
