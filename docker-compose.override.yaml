# Local development overrides loaded automatically by Docker Compose.

services:
  api-gateway:
    ports:
      - "8080:8080"

  rabbitmq:
    ports:
      - "15672:15672"

  postgres:
    ports:
      - "5432:5432"