spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:loandb-test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:loandb-test
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
    enabled: false
loan:
  events:
    exchange: loan.events
    borrowed-routing-key: loan.borrowed
    returned-routing-key: loan.returned
services:
  book:
    base-url: http://localhost:9999
